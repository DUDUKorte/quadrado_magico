#include <stdio.h>
#include <string.h>
#include "utils.h"




int main(){
    int size_x;
    int size_y;
    int init_pos = 2;
    int margin_size = 5;
    char line[1] = "-";
    scanf("%d %d", &size_x, &size_y);

    clrscr();
 
    //create_window(size_x, init_pos, init_pos, margin_size, line);

    return 0;
}